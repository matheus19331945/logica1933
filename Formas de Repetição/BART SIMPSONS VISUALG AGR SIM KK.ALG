Algoritmo "Bart SImpsons"
Var
num,bart:real
nome:caractere

Inicio

     escreval ("Digite um NOME : ")
     leia (nome)
     escreval ("Digite um NUMERO : ")
     leia (num)

     repita
     bart <- (bart + 1)
        escreval (nome,num)
     ate bart > 999


Fimalgoritmo